import { PanelStub } from './_stub';

export function McpPage() {
  return (
    <PanelStub
      title="MCP Registry"
      task="T605"
      description="Registered MCP servers, transports, and the auto-derived tool catalog."
    />
  );
}

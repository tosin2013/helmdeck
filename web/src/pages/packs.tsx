import { PanelStub } from './_stub';

export function PacksPage() {
  return (
    <PanelStub
      title="Capability Packs"
      task="T606 / T607"
      description="Browse the pack catalog, run the test runner, and inspect per-model success rates."
    />
  );
}

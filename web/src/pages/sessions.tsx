import { PanelStub } from './_stub';

export function SessionsPage() {
  return (
    <PanelStub
      title="Browser Sessions"
      task="T603"
      description="Live and historical browser session containers spawned by the runtime."
    />
  );
}
